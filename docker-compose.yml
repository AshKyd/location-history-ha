version: '3.8'

services:
  location-history:
    build: .
    container_name: ha-location-history
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/location_history.db
