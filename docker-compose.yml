services:
  location-history:
    build: .
    container_name: ha-location-history
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/location_history.db
