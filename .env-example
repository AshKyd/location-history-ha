################################
# Home Assistant Configuration
################################

# Base URL of your Home Assistant instance (no trailing slash)
HA_URL=https://your-instance.ui.nabu.casa

# Long-Lived Access Token (Create in HA Profile > Security)
HA_TOKEN=your_token_here

# Entity ID of the device tracker to follow
TRACKER_ID=device_tracker.pixel_9_pro

################################
# Script Configuration
################################

# Number of past days to fetch history for
# Set to 10 for a longer backfill.
HISTORY_DAYS=1

# Timezone for database storage (e.g. Australia/Brisbane)
TIMEZONE=UTC

# Cron Schedule (default: every hour)
# '*/5 * * * *' = Every 5 minutes
CRON_SCHEDULE='0 * * * *'

# SQLite Database Path
# For Local Run: ./location_history.db
# For Docker: /data/location_history.db
DB_PATH=/data/location_history.db

################################
# Sensor Configuration
# Format: HA_SENSOR_<COLUMN_NAME_UPPERCASE>=<entity_id>
################################
HA_SENSOR_BATTERY_LEVEL=sensor.example_battery_level
HA_SENSOR_WIFI_SSID=sensor.example_wifi_connection
HA_SENSOR_IS_CHARGING=binary_sensor.example_is_charging
